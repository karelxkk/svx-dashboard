#!/bin/sh
set -e

case "$1" in
    configure|reconfigure)
        # vytvořit /run a /var/log podle tmpfiles.d
        if command -v systemd-tmpfiles >/dev/null 2>&1; then
            systemd-tmpfiles --create >/dev/null 2>&1 || true
        fi
    ;;
esac

#DEBHELPER#

exit 0

