
# Automatically added by dh_python3
if command -v py3compile >/dev/null 2>&1; then
	py3compile -p svx-dashboard /usr/lib/svx-dashboard
fi
if command -v pypy3compile >/dev/null 2>&1; then
	pypy3compile -p svx-dashboard /usr/lib/svx-dashboard || true
fi

# End automatically added section
# Automatically added by dh_installtmpfiles/13.11.4
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
	if [ -x "$(command -v systemd-tmpfiles)" ]; then
		systemd-tmpfiles ${DPKG_ROOT:+--root="$DPKG_ROOT"} --create svx-dashboard.conf >/dev/null || true
	fi
fi
# End automatically added section
